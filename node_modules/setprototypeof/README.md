💬 Chat en Tiempo Real con Socket.IOEste proyecto implementa una aplicación de chat simple en tiempo real para demostrar el uso de la librería Socket.IO y la comunicación bidireccional entre el cliente y el servidor.🎯 Objetivo de la PrácticaEl objetivo principal fue comprender los fundamentos de los requests en tiempo real, estableciendo una conexión persistente para el intercambio inmediato de datos, a diferencia del modelo de petición/respuesta de HTTP tradicional.🧠 Conceptos ClaveEl corazón de esta aplicación es Socket.IO, una librería que utiliza WebSockets (y tecnologías de fallback) para mantener una conexión abierta entre el servidor (Node.js) y el cliente (Navegador).Comunicación Bidireccional: Tanto el cliente como el servidor pueden iniciar el envío de datos.Basada en Eventos: La comunicación se define mediante el envío y recepción de eventos personalizados (ej. chatMessage, newUser, disconnect).Tiempo Real: Permite el intercambio de datos de manera inmediata, ideal para chats y notificaciones.⚙️ Requisitos e InstalaciónPara ejecutar este proyecto, necesitas tener instalado Node.js y npm (que viene con Node.js).1. Clonar o Descargar el Proyecto(Si no lo has hecho, puedes saltar este paso)Bashgit clone <URL_DEL_REPOSITORIO_O_NOMBRE_DE_TU_CARPETA>
cd chat-realtime
2. Instalar DependenciasDesde la terminal en la carpeta principal, instala las librerías necesarias para el servidor (express y socket.io):Bashnpm install express socket.io
🚀 Uso de la Aplicación1. Iniciar el ServidorEjecuta el archivo server.js con Node.js:Bashnode server.js
Verás el mensaje de confirmación: 🚀 Servidor corriendo en puerto 3000.2. Iniciar el ClienteAbre tu navegador web y navega a la siguiente dirección:http://localhost:3000
Para probar la funcionalidad en tiempo real, abre al menos dos pestañas o ventanas en esta dirección.3. Probar el ChatEn cada pestaña, establece un nombre de usuario haciendo clic en "Guardar".Envía un mensaje desde una pestaña.Verifica que el mensaje aparezca instantáneamente en la otra pestaña.✅ Funcionalidades Implementadas (Parte 3)FuncionalidadDescripciónEvento ClaveMensajería en Tiempo RealEl servidor retransmite los mensajes a todos los clientes conectados.chatMessageNotificación de UsuarioSe notifica a todos cuando un usuario se conecta o desconecta.newUser, disconnect, notificationHistorial BásicoEl servidor mantiene un array con los últimos mensajes y los envía al cliente al conectarse.historyIdentificación de UsuarioEl nombre de usuario se muestra junto al mensaje.Integrado en el objeto de mensaje.Contador de Usuarios (Opcional)Muestra el número exacto de usuarios conectados actualmente.usersConnected📦 Estructura del Proyectochat-realtime/
├── node_modules/         # Dependencias
├── public/               # Código del Cliente (Frontend)
│   └── index.html        # HTML, CSS y JavaScript del Chat
├── server.js             # Lógica del Servidor (Node.js/Socket.IO)
├── package.json          # Metadatos y dependencias del proyecto
└── README.md             # Documentación del proyecto (este archivo)